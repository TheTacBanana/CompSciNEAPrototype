import random

class worldMap():
    def __init__(self, size):
        self.mapArray = [] * 10
        #for item in range(0, size - 1):
            #self.mapArray.append([0 for i in range(0, size - 1)])

        for y in range(0,59):
            for x in range(0,9):
                self.mapArray[2] = 1
                print(mapArray)